name: C++ CI

on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'log level'
      environment:
      tags:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  build:

    runs-on: [ ubuntu-latest, macos-latest ]

    steps:
    - name: configure
      run:
        cd ${{ github.workspace }}
        mkdir build
        cd build
        cmake -DCMAKE_BUILD_TYPE=Release ..

    - name: configure
      run:
        cd build
        ./pepsirf_test