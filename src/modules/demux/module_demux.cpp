#include "module_demux.h"

module_demux::module_demux() = default;

void module_demux::run( options *opts )
{
    options_demux d_opts = * ( (options_demux*) opts );

    // create parsers for the encoded library and the fastq reads.


    // 

}
